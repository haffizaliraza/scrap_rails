<%= form_with(model: scrap) do |form| %>
  <% if scrap.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(scrap.errors.count, "error") %> prohibited this scrap from being saved:</h2>

      <ul>
        <% scrap.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :keywords, style: "display: block" %>
    <%= form.text_area :keywords, style: "width: 100%; height: 150px; padding: 10px; font-size: 14px; border: 1px solid #ccc; border-radius: 5px; background-color: #f5f5f5; color: #333; resize: vertical;"%>
  </div>

  <div>
  <%= form.label :urls, style: "display: block" %>
  <%= form.text_area :urls, style: "width: 100%; height: 150px; padding: 10px; font-size: 14px; border: 1px solid #ccc; border-radius: 5px; background-color: #f5f5f5; color: #333; resize: vertical;" %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
